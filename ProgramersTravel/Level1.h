#include "Header.h";

using namespace std;

class Level1
{
public:
	vector<string> RunningRace(vector<string> players, vector<string> callings);
	vector<int> MemoryScore(vector<string> name, vector<int> yearning, vector<vector<string>> photo);
	vector<int> WalkInThePark(vector<string> park, vector<string> routes);
	vector<int> ScreenOrganization(vector<string> wallpaper);
	int Overpainting(int n, int m, vector<int> section);
	vector<int> BuiltInKeyBoard(vector<string> keymap, vector<string> targets);
	string BunchOfCards(vector<string> cards1, vector<string> cards2, vector<string> goal);
	string SecretCodeForTheTwo(string s, string skip, int index);
	vector<int> PersonalInformationCollectionPeriod(string today, vector<string> terms, vector<string> privacies);
	int KnightsWeapon(int number, int limit, int power);
	vector<int> TheNearestLetter(string s);
	int SeparatingStrings(string s);
};

